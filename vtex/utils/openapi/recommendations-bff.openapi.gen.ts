
// deno-fmt-ignore-file
// deno-lint-ignore-file no-explicit-any ban-types ban-unused-ignore
//
// DO NOT EDIT. This file is generated by deco.
// This file SHOULD be checked into source version control.
// To generate this file: deno task start
//        


export interface OpenAPI {
/**
 * Records when a user views a recommendation.
 * 
 * ## Permissions
 * 
 * This endpoint does not require [authentication](https://developers.vtex.com/docs/guides/authentication) or [permissions](https://help.vtex.com/en/tutorial/license-manager-resources--3q6ztrC8YynQf6rdc6euk3).
 */
"POST /api/recommend-bff/v2/events/recommendation-view": {
searchParams: {
/**
 * VTEX account name. Must match the account name in the `x-vtex-rec-origin` header.
 */
an: string
}
/**
 * Request body for recording a recommendation view event.
 */
body: {
/**
 * User ID.
 */
userId: string
/**
 * Correlation ID for the recommendation request.
 */
correlationId: string
/**
 * List of SKU IDs viewed.
 */
products: string[]
}
}
/**
 * Records a click on a product within a recommendation shelf.
 * 
 * ## Permissions
 * 
 * This endpoint does not require [authentication](https://developers.vtex.com/docs/guides/authentication) or [permissions](https://help.vtex.com/en/tutorial/license-manager-resources--3q6ztrC8YynQf6rdc6euk3).
 */
"POST /api/recommend-bff/v2/events/recommendation-click": {
searchParams: {
/**
 * VTEX account name. Must match the account name in the `x-vtex-rec-origin` header.
 */
an: string
}
/**
 * Request body for recording a recommendation click event.
 */
body: {
/**
 * User ID.
 */
userId: string
/**
 * Product ID that was clicked.
 */
product: string
/**
 * Correlation ID for the recommendation request.
 */
correlationId: string
}
}
/**
 * Retrieves a list of recommended products based on the provided campaign VRN (Virtual Resource Name), which identifies a specific recommendation campaign. The campaign determines the type of recommendations returned, such as personalized recommendations, similar items, cross-sell, and others.
 * 
 * 
 * ## Permissions
 * 
 * This endpoint does not require [authentication](https://developers.vtex.com/docs/guides/authentication) or [permissions](https://help.vtex.com/en/tutorial/license-manager-resources--3q6ztrC8YynQf6rdc6euk3).
 */
"GET /api/recommend-bff/v2/recommendations": {
searchParams: {
/**
 * VTEX account name. Must match the account name in the `x-vtex-rec-origin` header.
 */
an: string
/**
 * VRN identifier for an existing campaign, following the pattern: `vrn:recommendations:{store-name}:{campaignType}:{campaignId}`.
 * 
 * Available campaign types:
 * - `rec-top-items-v2`: Top selling or most popular items
 * - `rec-persona-v2`: Personalized recommendations based on user behavior
 * - `rec-similar-v2`: Similar items to a specified product
 * - `rec-cross-v2`: Cross-sell recommendations (complementary items)
 * - `rec-cart-v2`: Cart-based recommendations
 * - `rec-last-v2`: Last seen products for a user
 * - `rec-visual-v2`: Visually similar items
 * - `rec-search-v2`: Search-based recommendations
 * - `rec-next-v2`: Next interaction predictions based on user behavior
 */
campaignVrn: string
/**
 * User ID, required for personalized recommendations based on user behavior. It can be found in the `vtex-recommendations-user` cookie.
 */
userId?: string
/**
 * Comma-separated list of product IDs for context-based recommendations. For similar items and cross-sell, send only **one product ID**. For cart recommendations, send all product IDs in the user's cart.
 */
products?: string
/**
 * Trade policy ID. We also try to extract it from the segment cookie. If none is provided in the query, we will use the default trade policy.
 */
salesChannel?: string
/**
 * The user's locale information. If not provided, it is extracted from the segment cookie. This is important for multi-language stores.
 */
locale?: string
/**
 * Zip code for location-based recommendations. If not provided, it is extracted from the segment cookie.
 */
zipCode?: string
/**
 * Pickup point identifier for location-based recommendations. If not provided, it is extracted from the segment cookie.
 */
pickupPoint?: string
/**
 * Region identifier for location-based recommendations. If not provided, it is extracted from the segment cookie.
 */
regionId?: string
}
/**
 * Response containing recommended products and campaign information.
 */
response: {
/**
 * List of recommended SKU IDs.
 */
products: string[]
/**
 * Correlation ID for the recommendation request.
 */
correlationId: string
/**
 * Campaign information, if applicable.
 */
campaign: {
/**
 * Campaign ID.
 */
id: string
/**
 * Campaign title.
 */
title?: string
/**
 * Type of campaign. Supported campaign types:**
 * 
 *  - **TOP_ITEMS**: Returns the top selling or most popular items in the store.
 *  - **PERSONALIZED**: Provides recommendations tailored to the user's profile and behavior.
 *  - **SIMILAR_ITEMS**: Requires `products` with only **one product ID**. Returns items similar to the specified product.
 *  - **CROSS_SELL**: Requires `products` with only **one product ID**. Suggests complementary items to the specified product.
 *  - **CART_RECOMMENDATIONS**: Requires `products` with **all items currently in the user's cart**. Returns recommendations based on the cart contents.
 *  - **LAST_SEEN**: Returns the most recently viewed products for the given user.
 *  - **RECENT_INTERACTIONS**: Returns products the user has recently interacted with.
 *  - **VISUAL_SIMILARITY**: Returns visually similar items to the specified product.
 *  - **SEARCH_BASED**: Returns recommendations based on search behavior and patterns.
 *  - **NEXT_INTERACTION**: Predicts and suggests products the user is likely to interact with next.
 */
type: ("TOP_ITEMS" | "PERSONALIZED" | "SIMILAR_ITEMS" | "CROSS_SELL" | "CART_RECOMMENDATIONS" | "LAST_SEEN" | "RECENT_INTERACTIONS" | "VISUAL_SIMILARITY" | "SEARCH_BASED" | "NEXT_INTERACTION")
}
}
}
/**
 * Creates a unique user ID to associate with the current session and links it to the provided order form ID. This user ID is also stored in the `vtex-rec-user-id` cookie for session continuity. If the `orderFormId` is not provided in the request body, the API attempts to retrieve it from the `checkout.vtex.com` cookie. This endpoint is typically used to initialize a recommendation session for tracking and personalization purposes.
 * 
 * ## Permissions
 * 
 * This endpoint does not require [authentication](https://developers.vtex.com/docs/guides/authentication) or [permissions](https://help.vtex.com/en/tutorial/license-manager-resources--3q6ztrC8YynQf6rdc6euk3).
 */
"POST /api/recommend-bff/v2/users/start-session": {
searchParams: {
/**
 * VTEX account name. Must match the account name in the `x-vtex-rec-origin` header.
 */
an: string
}
/**
 * Request body for starting a user session.
 */
body: {
/**
 * Order form ID associated with the session.
 */
orderFormId: string
}
/**
 * Response body for starting a user session.
 */
response: {
/**
 * The recommendations user ID.
 */
recommendationsUserId: string
}
}
/**
 * Records when a user views a product. When source isn't passed, it defaults to `WEB_DESKTOP` or is derived from user agent.
 * 
 * ## Permissions
 * 
 * This endpoint does not require [authentication](https://developers.vtex.com/docs/guides/authentication) or [permissions](https://help.vtex.com/en/tutorial/license-manager-resources--3q6ztrC8YynQf6rdc6euk3).
 */
"POST /api/recommend-bff/v2/events/product-view": {
searchParams: {
/**
 * VTEX account name. Must match the account name in the `x-vtex-rec-origin` header.
 */
an: string
}
/**
 * Request body for recording a product view event.
 */
body: {
/**
 * User ID.
 */
userId: string
/**
 * Product ID viewed.
 */
product: string
/**
 * Source of the view event.
 */
source?: ("WEB_DESKTOP" | "WEB_MOBILE" | "MOBILE_APP" | "MOBILE" | "DESKTOP")
}
}
}
