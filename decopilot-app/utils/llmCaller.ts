// function callMessage(provider: LLMProvider, prompt: string) {
//     // isso aqui estara no seu ctx
//     const provider = providers[providerName.toLowerCase()];

//     if (!provider) {
//       throw new Error(`Unsupported provider: ${providerName}`);
//     }
//     provider.call(prompt);
//   }

// Usage
// callMessage("openai", "Hello, world!");
// callMessage("anthropic", "How are you?");
